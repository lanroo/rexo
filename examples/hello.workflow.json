{
  "schema_version": "0.1.0",
  "id": "hello",
  "version": "0.1.0",
  "steps": [
    {
      "id": "greeting",
      "capability": "text.constant",
      "needs": [],
      "with": { "value": "hello world" }
    },
    {
      "id": "shout",
      "capability": "text.uppercase",
      "needs": ["greeting"],
      "with": { "text": { "from_task": "greeting" } }
    },
    {
      "id": "doc",
      "capability": "text.template",
      "needs": ["shout"],
      "with": {
        "template": "# Welcome to REXO\n\nThis document was generated by a deterministic REXO workflow — no AI, fully reproducible.\n\nSteps that ran:\n\n1. `text.constant` produced \"hello world\".\n2. `text.uppercase` turned it into **{{shout}}**.\n3. `text.template` wrapped it into this file.\n\nEdit `workflow.json` and run `rexo run workflow.json` again to change this.\n\nWant real AI? Try: `rexo demo \"REST APIs\"`\n",
        "vars": { "shout": { "from_task": "shout" } }
      }
    }
  ],
  "outputs": {
    "welcome.md": "doc"
  }
}
